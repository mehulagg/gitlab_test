#import "./boards.fragment.graphql"

query project_boards($fullPath: ID!) {
  project(fullPath: $fullPath) {
    boards {
      edges {
        node {
          ...board
        }
      }
    }
  }
}
