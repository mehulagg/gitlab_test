#import "ee_else_ce/boards/queries/boards.fragment.graphql"

query group_boards($fullPath: ID!) {
  group(fullPath: $fullPath) {
    boards {
      edges {
        node {
          ...board
        }
      }
    }
  }
}
