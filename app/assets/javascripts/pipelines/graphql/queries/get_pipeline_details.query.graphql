query getPipelineDetails($projectPath: ID!, $iid: ID!) {
  project(fullPath: $projectPath) {
    pipeline(iid: $iid) {
      stages {
        nodes {
          name
          status @client
          groups {
            nodes {
              status @client
              name
              size
              jobs {
                nodes {
                  name
                  needs {
                    nodes {
                      name
                    }
                  }
                  status @client
                }
              }
            }
          }
        }
      }
    }
  }
}
