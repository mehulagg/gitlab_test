- active = params[:tab] == 'settings'

- if can_admin_cluster?(current_user, @cluster)
  %li.nav-item{ role: 'presentation' }
    %a.nav-link{ class: active_when(active), href: clusterable.cluster_path(@cluster.id, params: {tab: 'settings'}), id: 'cluster-settings-tab' }
      %span= _('Advanced Settings')
