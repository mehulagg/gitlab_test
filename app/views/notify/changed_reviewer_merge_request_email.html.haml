%p
  = _('Reviewer changed')
  - if @previous_reviewers.any?
    = _('from')
    %strong= sanitize_name(@previous_reviewers.map(&:name).to_sentence)
  = _('to')
  - if @merge_request.reviewers.any?
    %strong= sanitize_name(@merge_request.reviewer_list)
  - else
    %strong Unassigned
