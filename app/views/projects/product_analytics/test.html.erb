<%= render "links" %>
<p>
Project ID: <%= @project_id %>

Between 2.5 and 2.10 Snowplow goes from GET to POST requests.

You need to initialize the tracker and track an event to trigger a request.

This page sends a payload.
<script type="text/javascript">
<%= render "tracker" %>
</script>

<p>
<p>
Last item before this page loaded in your browser:
<p>
<%= @event.as_json.keep_if { |key, value| value }.inspect if @event %>
