query IterationBurnupTimesSeriesData($iterationId: ID!) {
  iteration(id: $iterationId) {
    burnupTimeSeries {
      nodes {
        date
        scopeCount
        scopeWeight
        completedCount
        completedWeight
      }
    }
  }
}
