mutation NamespaceIncreaseStorageTemporarily($input: NamespaceIncreaseStorageTemporarilyInput!) {
  namespaceIncreaseStorageTemporarily(input: $input) {
    errors
    namespace {
      id
      temporaryStorageIncreaseEndsOn
    }
  }
}
