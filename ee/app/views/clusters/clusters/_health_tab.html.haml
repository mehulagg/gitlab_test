- active = params[:tab] == 'health'

%li.nav-item{ role: 'presentation' }
  %a.nav-link{ class: active_when(active), href: clusterable.cluster_path(@cluster.id, params: {tab: 'health'}), id: 'cluster-health-tab' }
    %span= _('Health')
