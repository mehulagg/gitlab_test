import { shallowMount, mount } from '@vue/test-utils';
import StageTable from 'ee/analytics/cycle_analytics/components/stage_table.vue';

describe.skip('StageTable', () => {
  describe('with stages', () => {
    it('will render all the stages', () => {});
    it('will emit `selectStage` when a stage is selected', () => {});

    describe('isLoadingStage', () => {});
    describe('isEmptyStage', () => {});
    describe('with currentStage set', () => {});
  });
  describe('without no stages', () => {});
});
