# Gitlab::MigrationHelpers

## Development

To run tests, starting from `Rails.root`

```bash
$ cd vendor/gitlab-migration_helpers/

$ export GITLAB_MIGRATION_HELPERS_TEST_DATABASE_CONFIG=../../config/database.yml

$ bundle exec rspec
```
